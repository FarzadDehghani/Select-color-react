const Box = ({color}) => {
    return (
        <div
        style={{
          width: '100px',
          height: '100px',
          backgroundColor: `${color}`,
          marginLeft: '150px',
        }}
      ></div>
    )
}

export default Box